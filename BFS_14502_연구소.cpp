#include <iostream>
#include <queue>
#include <cstring>

using namespace std;

int n, m;
int d[10][10];
int c[10][10] = {0,};

void bfs(int x, int y){
    queue<pair<int,int>> q;
    
}
int main(){
    cin >> n >> m;
    
    memset(d,-1, sizeof(d));

    for(int i=0; i<n; i++){
        for(int j=0; j<m; j++){
            cin >> d[n][m];
        }
    }
}